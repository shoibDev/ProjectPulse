export interface Project {
  id: number;
  title: string;
  description: string;
  dateCreated: string;
  lastUpdated: string;
  userIds: number[];
  ticketIds: number[];
}

export interface User{

}

export interface Ticket{
  
}