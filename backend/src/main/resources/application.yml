spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/BugTrackerDb
    username: postgres
    password: Flower1973!@
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: create-drop
    show-sql: true  # Changed from false to true to enable SQL logging
